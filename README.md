You can directly run the code IM.py. Some packages such as matplotlib, networkx and numpy as required.
All the parameters in the file are the same as the paper;
After the run, the program will output the performances of 4 algorithms on 4 networks.
Totally 9 png file will also be output to visualize the process and the comparison.
